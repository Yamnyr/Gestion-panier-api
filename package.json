{
  "name": "api-mon-panier",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm run json-server",
    "forward": "adb reverse tcp:7000 tcp:7000",
    "json-server": "json-server --port 7000 --watch data.json",
    "delay-api": "npm run json-server -- --middlewares=delay.js",
    "api": "npm run json-server"

  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
